'use client';
import { useState } from 'react';
import { createClient } from '@supabase/supabase-js';
import Link from 'next/link';
const supabase=createClient(process.env.NEXT_PUBLIC_SUPABASE_URL!,process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!);
export default function Signup(){const [email,setEmail]=useState('');const [name,setName]=useState('');const [msg,setMsg]=useState('');async function submit(e:React.FormEvent){e.preventDefault();setMsg('Creating your secure account…');const {error}=await supabase.auth.signInWithOtp({email,options:{data:{full_name:name},emailRedirectTo:`${window.location.origin}/auth/callback`}});setMsg(error?.message||'Account started. Check your email to finish signing in.');}return <main className="shell section"><Link className="brand" href="/">Workflow<span>Forge</span></Link><div className="card" style={{maxWidth:520,margin:'70px auto'}}><h1>Start free</h1><p className="muted">No password to remember. Your account is secured by email magic link.</p><form className="form" onSubmit={submit}><label className="label">Your name</label><input className="input" required value={name} onChange={e=>setName(e.target.value)} placeholder="Alex Smith"/><label className="label">Business email</label><input className="input" type="email" required value={email} onChange={e=>setEmail(e.target.value)} placeholder="you@business.com"/><button className="button" type="submit">Create my account →</button></form>{msg&&<p className="muted">{msg}</p>}<p className="muted">Already have an account? <Link href="/login">Sign in</Link></p></div></main>}
