import Link from 'next/link';
const plans=[['Starter','$0','3 packs / month','Free forever'],['Pro','$19','Unlimited core workflows','Weekly automation'],['Studio','$49','Multiple businesses','Team-ready workspace']];
export default function Pricing(){return <main className="shell section"><Link className="brand" href="/">Workflow<span>Forge</span></Link><h1 style={{marginTop:60}}>Pricing that scales with the business.</h1><p className="muted">Start free. Upgrade when recurring marketing work is worth automating.</p><div className="grid" style={{marginTop:30}}>{plans.map(([name,price,desc,extra])=><article className="card" key={name}><span className="badge">{name.toUpperCase()}</span><h2>{name}</h2><div className="price">{price}</div><p>{desc}</p><p className="muted">✓ {extra}</p>{name==='Starter'?<Link className="button secondary" href="/signup">Start free</Link>:<Link className="button" href="/signup">Start {name} →</Link>}</article>)}</div></main>}
