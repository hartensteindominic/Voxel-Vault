export const metadata = {
  title: 'VoxelFlip Launch Status',
  robots: {
    index: false,
    follow: false,
    nocache: true,
    noarchive: true,
    nosnippet: true,
  },
};

export default function VoxelFlipLaunchLayout({ children }: { children: React.ReactNode }) {
  return children;
}
