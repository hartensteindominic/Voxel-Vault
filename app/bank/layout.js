export const metadata = {
  title: 'Vault One — Digital Banking Demo',
  description: 'A polished digital banking prototype with virtual cards, transfers, savings and activity views.',
};

export default function BankLayout({ children }) {
  return children;
}
