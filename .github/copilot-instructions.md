# VoxelPop Copilot / ChatGPT instructions

Read `AGENTS.md` and `CLAUDE.md` before editing.

This repository's public product is VoxelPop:

authorized house photo → $4.99 → 3D voxel photo → approval → movable voxel → Vault → optional mint.

Do not add extra gates. Do not break 3D. Homepage and demo 3D must use `/voxelpop/demo-house.jpg`. Never sample SVG with `naturalWidth || 1`. Keep the cream / purple / lime VoxelPop aesthetic. Digital collectible only — not a deed.
