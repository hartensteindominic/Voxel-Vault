name: Temporary Lockfile Refresh

on:
  push:
    branches: [fix/deterministic-lockfile-audit]

permissions:
  contents: write

jobs:
  refresh:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          ref: fix/deterministic-lockfile-audit
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Regenerate package-lock deterministically
        run: npm install --package-lock-only --ignore-scripts --no-audit --no-fund
      - name: Commit refreshed lockfile if needed
        shell: bash
        run: |
          if git diff --quiet -- package-lock.json; then
            echo "package-lock.json already synchronized"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add package-lock.json
          git commit -m "Synchronize npm lockfile"
          git push origin HEAD:fix/deterministic-lockfile-audit
