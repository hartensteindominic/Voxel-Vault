{
  "framework": "nextjs",
  "buildCommand": "npm run test:routes && npm run build",
  "regions": ["iad1"],
  "crons": [
    { "path": "/api/cron/catalog-3d", "schedule": "* * * * *" },
    { "path": "/api/cron/neural-core", "schedule": "*/30 * * * *" }
  ],
  "headers": [
    {
      "source": "/(.*)",
      "headers": [
        { "key": "X-Content-Type-Options", "value": "nosniff" },
        { "key": "Referrer-Policy", "value": "strict-origin-when-cross-origin" }
      ]
    }
  ]
}
