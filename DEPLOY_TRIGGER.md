# Voxel Vault deployment trigger

This branch-only file intentionally forces a fresh Vercel Preview deployment for `experiment/voxelflip-opensea` so the Preview runtime serves the current VoxelFlip first-mint flow and re-reads the current Vercel Preview environment variables.

This does not modify `main`, the protected VoxelPop FINAL, or the live production site.

VoxelFlip preview refresh requested for visible mint button, task recovery, and Supabase-free NFT preparation: 2026-08-25 15:54 UTC.
